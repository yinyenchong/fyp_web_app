<%= form_with(model: [complaint, complaint.complaint_replies.build]) do |form| %>
  
  <div class="field">
    <%= form.label :reply %>
    <%= form.text_area :reply %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
  
  
<% end %>
