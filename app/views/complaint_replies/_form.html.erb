<%= form_with(model: complaint_reply, local: true) do |form| %>
  <% if complaint_reply.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(complaint_reply.errors.count, "error") %> prohibited this complaint_reply from being saved:</h2>

      <ul>
        <% complaint_reply.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :reply %>
    <%= form.text_area :reply %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
